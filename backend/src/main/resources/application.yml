spring:
  profiles:
    active: dev

  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

  # Mail
  mail:
    host: smtp.163.com
    port: 465
    username: # 邮箱
    password: # SMTP 授权码
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
